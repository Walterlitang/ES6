变量作用域：
    1.函数内部可以使用全局变量
    2.函数外部不可以使用局部变量
    3.当函数执行完毕后，本作用域内的局部变量会销毁

闭包：指有权访问 访问 另一个函数作用域中 变量 的函数
    （闭包函数）（高级函数）

闭包的作用：延伸了变量的作用范围

立即执行函数也称为小闭包

递归函数：自己调用自己的函数就是递归函数
    递归函数和循环效果类似，由于递归很容易发生 "栈溢出"(么有终止条件，导致函数不断创建，不断开辟内存，最终内存不够用)
    所以必须加退出条件，例如：return 1;
